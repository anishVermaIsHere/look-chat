

services:
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: lookchat
  #   ports:
  #     - "5432:5432"
      
  backend: 
    container_name: look_chat_api
    build: 
      context: .
      dockerfile: Dockerfile
    ports: 
      - 8000:8000
    env_file:
      - .env
    depends_on:
      - redis


  redis:
    image: redis:latest
    container_name: redis_look_chat
    ports:
      - "6379:6379"